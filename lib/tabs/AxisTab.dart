import 'package:chg/axis.dart';
import 'package:flutter/material.dart';

class AxisTab extends StatelessWidget {
  const AxisTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Center(
          child: AxisPage(),
        ),
      ),
    );
  }
}
