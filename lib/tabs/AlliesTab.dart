import 'package:chg/allies.dart';
import 'package:flutter/material.dart';

class AlliesTab extends StatelessWidget {
  const AlliesTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: AlliesPage(),
      ),
    );
  }
}
